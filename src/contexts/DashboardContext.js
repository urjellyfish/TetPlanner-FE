/**
 * DashboardContext.js — creates and exports the Dashboard context.
 *
 * State and logic live in DashboardProvider.jsx.
 */
import { createContext } from "react";

const DashboardContext = createContext(null);

export default DashboardContext;
